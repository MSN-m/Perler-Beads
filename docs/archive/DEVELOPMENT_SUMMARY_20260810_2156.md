# 开发总结 2026-08-10 21:56

## 本轮目标

1. 检查并确认 GitHub 远程新增的实验版本内容。
2. 搭建扁平化算法对比实验页面。
3. 对比当前版本与“主体保护型扁平化 + 多候选颜色评分”中期方案。
4. 为中期方案提供可调参数，并修复生成卡顿和结果不显示问题。

## GitHub 与本地状态

- GitHub 远程新增提交：`329f3b2`。
- 提交时间：2026-08-10 16:53 +0800。
- 提交信息：`8.10` / “实验版，测试效果”。
- 远程新增文件：`algorithm-lab.html`、`src/algorithm-lab.js`。
- 远程新增内容与当时本地 5 个未提交文件没有文件级重叠。
- 当前工作区仍有未提交修改，尚未执行 commit 或 push。

## 已完成改动

### 扁平化对比实验室

- 新增 `flatness-lab.html`。
- 新增 `src/flatness-lab.js`。
- 左侧显示统一测试参数，右侧并排显示：
  - 当前版本。
  - 中期方案。
- 基础参数包括最大边长、拼豆品牌、最大颜色种类。
- 中期方案参数包括：
  - 主体优先阈值。
  - 轮廓保护强度。
  - 细节保留强度。
  - 颜色误差权重。
  - 邻域连续性权重。
- 页面显示颜色数量、豆子数量和耗时。
- 最大边长默认值调整为 100。

### 中期算法方案

- 在 `src/processor.js` 新增 `generatePatternDataProtected()`。
- 使用主体区域、中心区域、边缘强度和邻域连续性共同决定每个网格的颜色。
- 保留多候选颜色评分的结构。
- 增加最大颜色数限制，避免超过用户设置。
- 不替换正式生成器中的现有算法，仅供实验页面调用。

### 性能与错误修复

- 修复中期方案调用未定义 `colorDifference()` 导致结果不显示的问题。
- 优化中期方案：先汇总每个网格的颜色与边缘信息，再进行调色板评分，避免对每个原图像素重复计算 Lab 色差。
- 100×100 网格的合成测试耗时约 200ms，已不再出现长时间占用主线程的情况。
- 为实验页面加入图片读取失败和生成失败时的状态恢复，避免按钮永久禁用。
- 为 `processor.js` 增加实验页查询参数，降低旧模块缓存影响。

### 其他已存在的工作区修改

当前工作区还保留以下此前未提交的修改，未在本轮重新整理：

- `css/style.css`
- `index.html`
- `src/features/adjust.js`
- `src/main.js`
- `src/ui.js`

这些文件涉及 PAD 适配、工具互斥、弹窗行为和编辑交互等历史工作，详见上一份归档记录。

## 验证状态

- `src/processor.js` 通过 `node --check`。
- `src/flatness-lab.js` 通过 `node --check`。
- `git diff --check` 通过。
- 已在浏览器中验证图片上传和“生成对比结果”按钮流程。
- 已验证当前版本和中期方案都能生成结果。
- 浏览器曾出现旧模块缓存问题，重启本地预览服务后恢复。

## 已知问题

- `src/processor.js` 当前出现 LF/CRLF 混用提示，后续提交前应统一为项目约定的 CRLF。
- 中期方案目前属于实验实现，主体识别仍主要由局部边缘和中心权重近似，不是真正的语义分割。
- “主体优先阈值”当前使用的是局部特征比例，后续需要用真实图片进行参数标定。
- 实验页面仍依赖 Tailwind CDN，不适合作为生产构建方案。
- 当前未完成正式视觉评测，也未将截图和评分整理进最终测试表。
- `build_test_matrix_preview.mjs` 与 `outputs/` 是之前生成的 Excel 样式预览辅助文件，尚未清理或提交。

## 下一步计划

1. 使用几张代表性图片对中期方案参数进行实际调试。
2. 重点观察人物轮廓、眼睛、头发、动物面部和插画线稿。
3. 对比当前版本与中期方案的颜色数量、轮廓完整度和实际拼制难度。
4. 根据测试结果调整主体阈值、边缘强度和连续性权重。
5. 统一新增文件与 `src/` 文件的换行格式。
6. 完成浏览器视觉验证后，再决定是否把中期方案合并进正式处理流程。
7. 确认无误后再统一 commit 和 push 到 GitHub。

