# 开发进度总结 (Development Summary) - 20260303_0000

## 1. 本次变更摘要
- 新增「颜色调整」模式工作流：首次点击选择接收格，二次点击选择来源格，接收格随即变色，支持多次操作，并提供撤回、取消、确认三种控制。
- 删除旧版吸管功能与 UI，统一改为「颜色调整」模式，交互更明确，误触概率更低。
- 新增批量换色能力（在预览页颜色清单的菜单中）：
  - 从图纸替换颜色：选择清单项色号为被替换色 A，点击画布上的颜色 B，图纸中全部 A → B（进入调整会话，支持撤回/取消/确认）。
  - 更换相近颜色：自动计算与 A 最相近的 3 个候选色，支持在三色间反复预览，确认后一次性应用，取消则不生效。
- 相近颜色选取严格限定于「当前品牌 + 当前套装过滤后的色盘」，并使用 Redmean 距离从该集合中选取 Top-3；基准色优先取该套装的同色号 RGB，若不存在则用当前清单项的 RGB。
- 预览交互微调：进入调整模式后画布光标切为十字，禁用拖拽；退出模式恢复抓手。
- 颜色清单布局优化：色号与数量同列并排（整体左对齐），右侧预留菜单按钮。
- 菜单层级与可见性修复：提升菜单 z-index，打开时临时提升父项层级；全局互斥开启/关闭，点击清单区外可关闭；避免被相邻项遮挡。
- 保持菜单面板稳定：进入调整模式不再立即重建颜色清单，避免菜单在打开后被销毁。

## 2. 关键修改文件
- [index.html](file:///d:/AIcode/Perler-Beads/index.html)
  - 预览页顶栏新增「颜色调整 / 撤回 / 取消 / 确认」按钮，移除吸管按钮。
- [state.js](file:///d:/AIcode/Perler-Beads/src/state.js)
  - 新增/维护调整模式状态：editMode、adjustPhase、receiverIndex、stagedPixelData、stagedActions、preAdjustZoomState。
  - 新增批量替换上下文：batchReplace（active/mode/sourceColorId/nearCandidates/nearBaseline/nearCurrentId）。
- [main.js](file:///d:/AIcode/Perler-Beads/src/main.js)
  - 调整模式下禁用拖拽与触摸移动；点击事件在调整模式中路由至编辑处理逻辑。
- [ui.js](file:///d:/AIcode/Perler-Beads/src/ui.js)
  - 颜色清单重绘为「左侧文案、右侧菜单按钮」结构，并为菜单层级与事件做了互斥与防冒泡处理。
  - 新增/调整核心逻辑：
    - toggleAdjustMode：进入/退出调整会话（不在进入时重建清单）。
    - handleResultCanvasClickForAdjust：接收格/来源格点击替换；支持 from-canvas 批量替换。
    - performBatchReplace：批量替换到暂存缓冲并生成一条撤回记录。
    - redmeanDistance、getCurrentPalette：相近颜色计算与当前套装过滤。
    - 扩展 adjustUndo：支持批量撤回（indices + prevColors）。

## 3. 重要功能说明
- 调整模式（单格）：  
  选接收格 → 选来源格 → 即时更新暂存像素 → 可撤回/取消/确认。
- 批量替换（从图纸）：  
  选清单项 A → 画布点选 B → 全量 A → B（暂存）→ 撤回/取消/确认。
- 批量替换（相近颜色）：  
  Top-3 候选限制于当前品牌+套装 → 小面板反复预览 → 确认落地或取消还原。

## 4. Bug 修复
- 菜单层级不稳定被遮挡：统一提升菜单及父项 z-index，并在打开前关闭其他菜单；菜单内部阻止冒泡；点击清单区外关闭所有菜单。
- 菜单按钮可见性：改为固定尺寸 SVG 图标按钮，加可视化兜底样式；父容器 overflow-visible 以保证弹层不被裁剪。
- 相近颜色不相近：候选范围严格限定为当前品牌+当前套装过滤后的色盘，基准 RGB 优先取套装版本，距离采用 Redmean 排序 Top-3。
- 相近颜色面板不出现：进入调整模式不再重建清单，防止刚打开菜单即被销毁；点击「更换相近颜色」时强制保持菜单展开并显示候选小面板。

## 5. 已知问题与建议
- 菜单在极端小屏/长列表滚动时可能发生视口外溢；可进一步将菜单挂载至 body 并做全局定位（Portal）以彻底规避。
- 相近颜色算法可升级为 Lab/CIEDE2000 以更贴近人眼感知（当前 Redmean 已较优，但在某些色域仍可能主观不理想）。
- 若需要在预览阶段展示「预计统计」以便评估替换影响，可在小面板显示实时统计变化（当前为确认后更新统计）。

## 6. 下一步计划
- 引入 Lab/CIEDE2000 作为可选相似度度量，提升相近色候选质量。
- 菜单 Portal 化与视窗自适应定位，进一步增强稳定性与可用性。
- 清单项小面板中展示候选色的 Δ 值/色号，辅助用户判断。
- 为调整模式与批量替换补充小提示与键盘快捷键（如撤回 Ctrl+Z）。

## 7. 变更清单（主要）
- 新增：调整模式（单格替换、批量替换 from-canvas、相近颜色三色预览）、撤回/取消/确认。
- 移除：旧版吸管模式与 UI。
- 修复：菜单 z-index 与可见性、进入调整模式导致菜单销毁、相近颜色候选来源不准确。

---
*Generated by Trae AI at 2026-03-03*
