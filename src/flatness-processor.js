const fp=id=>document.getElementById(id);let fpSource,fpResult;
function fpDraw(c,d){c.width=d.width;c.height=d.height;c.getContext('2d').putImageData(d,0,0)}
function fpFlat(d){let o=new ImageData(d.width,d.height),k=23;for(let y=0;y<d.height;y++)for(let x=0;x<d.width;x++){let i=(y*d.width+x)*4,r=0,g=0,b=0,n=0;for(let yy=Math.max(0,y-2);yy<=Math.min(d.height-1,y+2);yy++)for(let xx=Math.max(0,x-2);xx<=Math.min(d.width-1,x+2);xx++){let j=(yy*d.width+xx)*4;if(d.data[j+3]>127){r+=d.data[j];g+=d.data[j+1];b+=d.data[j+2];n++}}if(d.data[i+3]>127){o.data[i]=Math.round(r/n/k)*k;o.data[i+1]=Math.round(g/n/k)*k;o.data[i+2]=Math.round(b/n/k)*k;o.data[i+3]=255}}return o}
window.addEventListener('load',()=>{fp('flat-image-input').addEventListener('change',e=>{let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{let im=new Image();im.onload=()=>{let c=document.createElement('canvas');c.width=im.naturalWidth;c.height=im.naturalHeight;c.getContext('2d').drawImage(im,0,0);fpSource=c.getContext('2d').getImageData(0,0,c.width,c.height);fpDraw(fp('flat-source-canvas'),fpSource);fp('flat-generate-btn').disabled=false;fp('flat-status').textContent='图片已载入，可以开始扁平化。'};im.src=r.result};r.readAsDataURL(f)});fp('flat-generate-btn').addEventListener('click',()=>{fpResult=fpFlat(fpSource);fpDraw(fp('flat-result-canvas'),fpResult);fp('flat-download-btn').disabled=false;fp('flat-status').textContent='处理完成，可下载 PNG。'});fp('flat-download-btn').addEventListener('click',()=>{let a=document.createElement('a');a.download='perler-flat-medium.png';a.href=fp('flat-result-canvas').toDataURL('image/png');a.click()})});
